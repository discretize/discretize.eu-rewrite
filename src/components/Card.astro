---
export interface Props {
  title: string;
  body: string;
  href: string;
}

const { title } = Astro.props;
---

<article class="card">
  <h5>{title}</h5>
  <slot />
</article>
<style>
  .card {
    margin-bottom: 1rem;
    overflow: hidden;
    text-overflow: ellipsis;
  }
</style>
