---
interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<ul class={`list-disc list-outside ml-4 pl-4 ${className}`}>
  <slot />
</ul>

<style>
  ul:not(ul ul) {
    margin-bottom: 1rem;
  }
</style>
