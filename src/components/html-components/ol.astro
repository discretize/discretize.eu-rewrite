<ol class="mb-4 pl-0 m-0">
    <slot />
</ol>

<style is:global>
    ol {
        list-style-type: none;
        counter-reset: section;
    }
    ol ol {
        margin-bottom: 0rem !important; /* needed to lower gap for nested ols */
    }

    ol > li {
        padding-top: 0.75rem;
        display: flex;
    }

    ol > li::before {
        counter-increment: section 1;
        content: counters(section, ".");
        --tw-bg-opacity: 1;
        background-color: hsl(
            var(--b1) / var(--tw-bg-opacity)
        ); /* rgb(42, 44, 49); */
        border-radius: 4px;
        box-shadow: rgba(0, 0, 0, 0.2) 0px 2px 1px -1px,
            rgba(0, 0, 0, 0.14) 0px 1px 1px 0px,
            rgba(0, 0, 0, 0.12) 0px 1px 3px 0px;
        padding: 2px 6px;
        border: 1px solid rgb(30, 33, 36);
        margin-right: 8px;
        margin-left: 8px !important;
        margin-bottom: auto;
    }
</style>
