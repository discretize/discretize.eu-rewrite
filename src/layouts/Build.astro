---
import BuildFrontmatter from "../components/BuildFrontmatter.astro";
import Layout from "./Layout.astro";
2;
export interface Props {
  title: string;
}

const { title } = Astro.props.frontmatter;
---

<Layout title={title}>
  <BuildFrontmatter />
  <slot />
</Layout>
